# 김진완 | Frontend Engineer

- [포트폴리오](portfolio.html) · [블로그](https://crowwan.github.io/) · [깃허브](https://github.com/crowwan)

---

## 자기소개

**AI로 빠르게 개발하면서, 그 변화를 검증으로 안전하게 받치는 프론트엔드 개발자입니다.**

2023년 9월부터 AI 기반 치과 CAD/CAM SaaS인 Dentbird의 프론트엔드를 맡아, 구독·결제와 3D 뷰어, 데스크톱 CAM 연동처럼 외부 의존이 많은 복잡한 화면을 만들고, 그 화면을 받치는 모노레포·환경 분리·품질 자동화까지 주도적으로 다뤄 왔습니다.

AI는 코드 자동완성을 넘어 변경 감지·테스트 선별 같은 판단 단계에 결합해, 빠르게 바뀌는 코드도 안전하게 검증하는 워크플로우를 직접 만들었습니다. 만성적으로 실패하던 결제 경로 테스트를 원인별로 분리 진단해 28건에서 0으로 줄인 것처럼, 문제는 빠른 처치보다 근본 원인을 정확히 파악한 뒤 해결합니다. 새로운 기술은 트레이드오프를 정량적으로 따져 가치가 분명할 때 도입하고, 만든 뒤에도 실제로 효과가 있었는지 되짚어 의미가 없으면 인정하고 방향을 바꿉니다. AI가 내놓은 결과도 사실로 검증해, 판단은 직접 내립니다.

좋은 개발은 팀이 같은 방향을 공유할 때 나온다고 보아, 성과뿐 아니라 되돌린 결정과 한계까지 정직하게 남기려 합니다.

---

## 경력

**이마고웍스 (ImagoWorks)** | 프론트엔드 개발자 | Dentbird 1 Team | 2023.09 ~ 현재 (약 2년 9개월)

AI 기반 치과 CAD/CAM SaaS인 Dentbird의 프론트엔드를 맡아, 웹·데스크톱 앱 구현부터 모노레포 통합·런타임 환경 분리·품질 자동화 같은 플랫폼 기술 결정까지 담당해 왔습니다.

- 구독·결제 도메인 프론트엔드 전담, FSD·선언적 에러 처리 아키텍처 설계
- 여러 클라이언트 앱·라이브러리를 NX 모노레포로 통합(Git Subtree 이관)·환경·도메인 구성 일원화
- 격리 재현 환경과 AI 변경 감지 기반 품질 자동화·E2E 인프라 구축
- 5개 앱 에러 관측·복원력 표준화(Datadog), 3D 렌더 정합 재설계·시각 회귀 테스트
- Electron 로컬 연동 앱(Linker) 단독 개발·운영, 빌드·서명·배포 파이프라인 자동화
- 기업 랜딩 v3 풀스택 단독 리뉴얼 — 성능·SEO·타입세이프 i18n·백오피스

---

## 주요 프로젝트

> 각 프로젝트의 배경·대안 비교·회고는 [포트폴리오](portfolio.html)에 정리했습니다.

### 계정·구독 클라이언트 아키텍처 (FSD·컴포넌트 패턴)

계정·구독 클라이언트에 FSD·컴포넌트 패턴·선언적 에러 처리를 도입하고 팀 적용 기준을 정립했습니다.

- FSD로 도메인 분리, Compound Component·Render Props로 관심사 분리 — **단점·적용 한계까지 문서화**
- **ErrorBoundary 기반 선언적 에러 처리** + 화면 단위 Fault Tolerance 설계, BusinessLogicError 커스텀 분기
- 3개 앱에 중복되던 CaseInfo 다이얼로그를 공통 컴포넌트로 추출 — **약 66% 감소**

---

### 케이스 목록 썸네일 로딩 최적화

사용자가 가장 자주 보는 케이스 목록·뷰어 화면의 렌더링 성능을 최적화했습니다.

- "N장 = 네트워크 N요청"이 되기 쉬운 썸네일을 **IntersectionObserver로 보일 때만 로드**하고, 동시에 들어온 요청을 모아 **한 번에 배치 요청**으로 처리
- 운영 중 발생한 무한 재요청을 재시도로 억제하는 대신, **재마운트마다 훅·캐시가 소실되는 구조**를 진단해 캐시를 공유 스코프로 끌어올리고 **성공뿐 아니라 실패도 캐시**해 근본 해결, 회귀 테스트로 고정

---

### 웹·로컬 CAM 연동 Electron 앱 (Dentbird Linker)

웹 보철 케이스를 사용자 PC의 CAM 소프트웨어로 전달하는 사내 연동 앱을 단독 설계·운영했습니다.

- Chrome LNA 정책으로 막힌 로컬 서버 통신을, 장기 안정성 기준의 대안 비교 끝에 **Custom Protocol 중간 레이어로 재설계**
- 좌표계·파일 전달 방식이 제각각인 **외부 CAM 12종을 단일 변환·연동 인터페이스로 통합**
- 명세 없던 기존 CAM 연동을 **특성화 테스트로 고정한 뒤 재구현**
- 설치 감지는 **틀려도 사용자가 막히지 않게** 비차단 안내로 통일하고, CAM 없이 시나리오를 재현하는 Mock Server와 Datadog 로그로 검증·진단

---

### 프론트엔드 모노레포 통합

여러 클라이언트 앱과 공용 라이브러리를 단일 NX 모노레포로 통합하고, 환경·도메인 구성을 일원화했습니다.

- 환경별 도메인·URL 구성을 **단일 통합 라이브러리로 일원화** — 팀의 런타임 환경 분리·격리 재현이 이를 기반으로 동작
- 별도 레포 클라이언트 앱 2종·공용 라이브러리 6종을 **Git Subtree로 이관**
- NX 자동 타겟 추론이 일으키던 **메모리 부족 빌드 실패**를 진단, 불필요한 산출물 설정을 제거해 해소

---

### 격리 재현 환경과 AI 변경 감지

테스트가 거의 없던 제품에 격리 재현 기반의 자동 회귀 감지 체계를 구축했습니다.

- 커밋 시점 클라이언트·서버·DB를 컨테이너로 묶어 **결정론적 격리 재현 환경 구성**
- 백오피스 전 영역에 E2E를 구축하며 **로그인 중복을 공통 헬퍼로 약 93% 감소**, 여러 레포에 나뉜 Playwright E2E를 모노레포로 통합·Page Object 리팩토링으로 중복 제거
- 커밋 변경을 분석해 연관 QA 테스트 케이스만 자동 선별·실행하고, **실패를 실제 회귀와 테스트 코드 문제로 1차 분류**해 Teams로 보고하는 AI 변경 감지를 EC2에 구축(10분 간격 크론잡 자동 실행)
- 개발팀 공용 도구로, **품질 테스트 케이스(TC)가 E2E로 검증됐는지 추적하는 대시보드**를 구축하고 E2E 통과 결과가 자동 반영되도록 연동 (FE 화면 + API)

---

### 3D 렌더링 품질과 시각 회귀 테스트

팀이 AI로 진행한 사내 3D 라이브러리→Three.js 마이그레이션에서, 이미지 맞춤으로 미세조정 값에 기대 있던 색·조명을 원본 엔진 동작 기준으로 표준 기능화했습니다.

- 이미지 맞춤으로 들어간 미세조정 값을 원본 엔진 실제 동작 근거로 **Three.js 표준 기능으로 대체** — 약 1,420줄 정리
- 격리 컨테이너에서 baseline을 고정 생성해 환경 차를 제거하고, **Playwright 스크린샷 비교로 3D 렌더 회귀를 검증**

---

### 데스크톱 앱 빌드·서명·배포 자동화

담당자 로컬 PC에서 돌던 두 데스크톱 앱의 빌드·서명·배포 파이프라인을 재설계했습니다.

- macOS 빌드 파이프라인 재설계로 전체 33~39분에서 **17~24분으로 단축** — PR 실측 약 -56%, 아티팩트 757MB→334MB
- Windows 코드 서명 에이전트를 물리 빌드머신에 직접 설치·풀 등록해 **단독 운영** — 서명 USB·인프라팀 의존 제거

---

### B2B 구독·결제 시스템 프론트엔드

일회성 크레딧에서 반복 구독으로 전환하는 시점에, 글로벌 멀티테넌트 결제·권한 도메인 프론트엔드를 전담했습니다.

- 플랜 업그레이드·시트 구매·구독 취소·재개·쿠폰·결제수단·결제 히스토리까지 구독 워크플로우 전체를 구현
- 결제 상태를 **서버·Stripe 동기화 기준**으로 두고, 무한 반복 방지 폴링·origin 격리된 결제 팝업으로 외부 연동 복원력 확보
- 모든 에러를 unknown으로 일괄 처리하던 것을 **예측 가능/불가능으로 구분** — 비즈니스 에러는 코드별 분기, 시스템 실패는 ErrorBoundary로 격리하고, 이 문제의식을 이후 **5개 앱 관측·복원력 표준화**로 확장

---

## 기술 스택

| 분류 | 기술 |
|------|------|
| **Language** | TypeScript |
| **Frontend** | React, Next.js, Electron |
| **State / UI** | TanStack Query, Recoil, MUI |
| **Build / Arch** | NX, Vite, Webpack |
| **3D / Graphics** | Three.js |
| **Testing** | Playwright, Jest |
| **CI/CD · Infra** | GitHub Actions, Docker, AWS |
| **Monitoring** | Datadog |

---

## 교육

**세종대학교** | 데이터사이언스학과 (학사) | 2017.03 ~ 2024.08
