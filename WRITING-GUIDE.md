# 이력서·경력기술서 재작성 스타일 가이드

> `resume.md`(짧은 이력서)와 `career-description.md`(상세 경력기술서)를 **다시 쓸 때의 기준 문서**입니다.
> 웹의 한국 IT 채용 가이드 + 글로벌 resume best practice + 실제 공개 사례를 종합했습니다 (출처는 맨 아래).
> 이 가이드는 [CLAUDE.md](./CLAUDE.md)의 **"발굴 OK / 발명 NO"**, **문체 룰**, **4축 인터뷰**와 정합하도록 설계됐습니다. 충돌 시 CLAUDE.md가 우선합니다.

대상: 미들급 프론트엔드 개발자(약 2년 9개월, 2023.09~), 치과 CAD/CAM SaaS(DentBird) 도메인. 이직 목표 라인 / 오늘의집 / CJ ENM.

---

## 0. 가장 중요한 한 가지

> **서사는 "문단"이 아니라 "구조"로 드러낸다. 각 불릿은 [한 줄 행동·결정] + *(괄호 안 수치·효과)* 로 압축한다.**

"문제 → 해결 → 임팩트"는 여전히 핵심입니다. 단 그걸 **긴 `~했습니다` 문단**으로 풀면 스캔이 안 되고 무겁습니다. 대신 **역할을 분리**합니다:

1. **카드 도입부 1~2문장** = [문제 + 택한 방향] (왜 이걸, 왜 이렇게)
2. **불릿** = [동작으로 시작한 행동/결정] *(효과·수치는 괄호로 분리)* — 한 불릿 한 줄
3. **카드 끝 "판단·지속성" 한 줄** = [트레이드오프 / 한계 → 재설계 / 지금도 유지] — **우리의 차별점, 카드마다 1줄 강제**

> **왜 이 구조인가**: 정주연 류 잘 쓴 이력서는 불릿이 짧고 수치가 괄호로 튑니다(`(작업 소요시간 70% 절감)`). 우리는 거기에 **판단 한 줄**을 더해 미들급 차별화를 얻습니다 — 단, 판단도 문단이 아니라 **한 줄로 압축**합니다.

> **긴 STAR 풀스토리·구어체 회고는 `interviews/`(면접 스크립트)에 두고, 이력서는 압축합니다.** interviews/findings 원문을 그대로 옮기지 않습니다(§4-5).

---

## 0-1. 포지셔닝 — "나를 한 문장으로"

이력서 전체를 관통하는 **한 문장 정체성**을 먼저 정합니다. 이게 없으면 프로젝트가 평면 나열되고, 면접관은 *"그래서 이 사람이 뭘 제일 잘하는데?"*에 답을 못 얻습니다. **"다 잘해요"는 "특별한 게 없다"로 읽힙니다.**

강점이 여러 개일 때는 버리지 말고 **우산 + 축 구조**로 위계를 만듭니다:
- **우산(메인 정체성)**: 한 문장. 모든 카드가 이걸 증명.
- **축(2~4개)**: 우산을 떠받치는 근거 영역. 각 카드는 어느 축에 속하는지 분명해야 함.

> **현재 포지셔닝 1안 (검증 대상 — 평가 루프에서 확정/조정)**
> - **우산**: "제품의 구조와 품질을 끝까지 책임지는 프론트엔드 개발자"
> - **축**: **범위**(FE+데스크톱+빌드 인프라+백엔드 일부) · **구조**(모노레포·MFE·도메인 설계) · **판단**(LNA 20여 방안 분석·trade-off·라이브러리 내부) · **효율**(AI 변경감지·E2E·빌드 파이프라인)

**적용**: 자기소개 첫 줄 = 우산. 핵심역량·카드 정렬 = 축 순서. 각 카드 첫 bullet = 그 축의 가장 강한 증거. (포지셔닝이 흔들리면 매력도 흔들리므로, 평가 루프 항목 1순위로 점검)

---

## 1. 두 문서의 역할 분리

| 항목 | `resume.md` (이력서) | `career-description.md` (경력기술서) |
|------|------|------|
| 목적 | "무엇을 했다" — 6초 스캔용 | "어떻게 했고 결과가 어땠나" — 증명용 |
| 분량 | 1~2페이지 | A4 1~3페이지 |
| 단위 | 키워드·한 줄 요약 | 프로젝트당 5~10줄 서사 |
| 담는 것 | 한 줄 소개, 경력 요약, 핵심역량, 대표 성과 1~2줄, 기술스택, 링크 | 배경/역할/기여/성과 풀 서술, **트레이드오프**, 정량 지표 |
| 빼는 것 | 사진·생년월일·주소, 빽빽한 표, 모호한 스킬 나열 | 직무 무관 경력, 팀 성과 뭉뚱그림 |

**철칙**: 둘을 병합 제출하지 않습니다. 프로젝트는 **3~5개로 압축**(난사 금지).

### 1-1. 어떤 프로젝트를 남기고 어떤 순서로 둘까 (선별·정렬)

"3~5개로 압축"에서 **무엇을 남길지**가 첫 실전 질문입니다. 다음 우선순위로 선별·정렬합니다:

1. **타겟 직무 연관도** (1순위) — 지원 공고의 핵심 요구와 가장 맞닿은 프로젝트. JD가 "대규모 트래픽·성능"을 원하면 성능 카드가, "플랫폼 아키텍처"를 원하면 모노레포/MFE 카드가 위로.
2. **임팩트 크기** (2순위) — 기술 의사결정의 깊이, 정량 성과, 영향 범위(팀/플랫폼 단위)가 큰 것.
3. **최신성** (3순위) — 비슷하면 최근 경험을 위로 (기억이 생생할수록 면접 방어력이 높음).

**정렬**: 첫 카드는 "타겟 연관도 × 임팩트"가 가장 큰 것을 둡니다 — 리크루터가 첫 카드를 가장 오래 봅니다.
**제외·축소**: 타겟 연관도가 낮고 임팩트가 작은 카드(단순 기여·유지보수성)는 이력서에서 빼거나 경력기술서 후미에 한 줄로 줄입니다.

> 진완님 케이스: 현재 8개 카드(Solutions / MFE / Account / Linker / Batch / Electron 인프라 / 랜딩 / Design System) 중, 짧은 `resume.md`에는 타겟 연관도·임팩트 상위 3~5개만 올립니다. 랜딩·Design System은 온보딩·기여 성격이라 후순위입니다. 이 선별은 §13(JD 매핑)에서 공고가 정해질 때 다시 평가합니다.

---

## 2. 경력기술서 섹션 구조 (권장 순서)

```
1. 한 줄 소개 (태그라인)
2. 경력 요약 (4~5줄)
3. 핵심 역량 (공고 키워드 정렬)
4. 주요 프로젝트 경험          ← 본문, 비중 최대
5. 기술 스택
6. 링크 (GitHub / 블로그 / 포트폴리오)
```

- **한 줄 소개(태그라인)**: `"N년차 프론트엔드 | [도메인 경험] + [강점 한 줄]"`
  예) `"프론트엔드 개발자 | React·TypeScript·Electron | 치과 CAD/CAM SaaS 플랫폼 아키텍처·품질 자동화 주도"`
- **경력 요약 4~5줄**: ① 주요 도메인·경험 ② 리딩/주도 경험 ③ 강점(품질·성능·아키텍처) ④ 현재 관심 방향. **추상적 인성어("성실·소통") 금지.**

---

## 3. 프로젝트 카드 표준 포맷 (★ 새 표준 — 그룹핑 압축형)

판단이 풍부한 **핵심 카드**는 소제목으로 그룹핑합니다(윤해민·이예서 류 벤치마크):

```
**[프로젝트명]** — 한 줄 정의
`기간` · 역할(개인 기여 명시)
(※ 카드 제목에 *(효율·구조)* 같은 축 라벨은 달지 않음 — 포지셔닝 4축은 자기소개에서만 표현)

[배경 1~2문장] — 문제 + 택한 방향. 담백하게, 인터뷰 말투 X.

▸ 기여
- [동작 명사 시작] *(지표 A → B · 효과)*      ← 가장 강한 것 먼저
- [행동/결정] *(효과·수치)*

▸ 판단·정책
- [대안 비교 → 선택] — 구체 트레이드오프 ("X 빠르나 Y라 탈락")
- 지속성: [지금도 유지 / 한계 → 재설계] 한 줄

`기술` Electron · Vite · Custom Protocol · draco3d · Datadog
```

**규칙**:
- **그룹은 카드 크기에 맞춰.** 판단 풍부 카드(Solutions·Linker·MFE)는 `▸ 기여` + `▸ 판단·정책` 2그룹. 판단 적은 작은 카드(랜딩·DS)는 그룹 없이 불릿 3~4개 + 기술 줄로 **담백하게**(이예서식).
- **배경 1~2문장.** "배경/역할/기여/성과" 4소제목으로 문단을 늘리지 않습니다.
- **불릿은 한 줄.** 방법은 본문, 효과·수치는 *(괄호)*. 한 불릿에 배경+방법+결과를 욱여넣지 않습니다.
- **기술 스택은 카드 하단 `기술` 줄로 분리**(이예서식). 헤더엔 두지 않습니다.
- **판단·정책 블록 = 우리 최대 차별점.** 단 "X도 고려했으나 Y라 Z" 구체형으로 — 추상 회고("~체득했습니다") 금지(§4-5).

> `resume.md`는 더 압축 — 카드당 도입 1문장 + 불릿 3~4개, 판단은 가장 강한 카드만 한 줄. `career-description.md`는 위 풀 포맷.

---

## 4. bullet 작성 공식

### 4-1. 압축 불릿 형식 (★ 기본형)

> **`[동작 명사로 시작한 행동/결정] *(지표 A → B · 도구·효과)*`**

수치·효과는 **괄호로 분리**해 문장 속에 묻지 않습니다. 한 불릿은 **한 줄**(최대 한 줄 반). **단 괄호엔 수치·효과·짧은 단서만** — 동작 흐름(A→B→C 메커니즘 설명)이나 긴 부연을 괄호에 욱여넣지 않습니다. 그건 본문에 녹이거나(화살표·대시) 면접 구두용으로 남깁니다.

- "환경변수 빌드타임 → **런타임 주입** 전환 *(빌드 1개를 dev·qa·prod 공용 → 핫픽스 반복 해소)*"
- "Playwright E2E 구축, 로그인 셋업 *(60줄 → 공통 호출 1줄)*, 세션 공유로 스위트 *(18분 → 11분, 39%↓)*"
- "모노레포 통합 *(TS 에러 688 → 0)*, Facade로 토큰 관리 코드 *(200 → 33줄, 85%↓)*"
- "번들러 Webpack → Vite 마이그레이션 *(서버 구동 85%↓, HMR 대기 100%↓)*" ← 정주연 류 벤치마크

### 4-2. 방법은 본문, 수치는 괄호 — 묶지 않기

| ❌ 묻힌 수치 (긴 서술) | ✅ 분리된 수치 (압축) |
|---|---|
| "테스트마다 반복되던 로그인 셋업 코드(약 60줄)를 공통 Page Object 호출로 대체해 중복을 93% 제거하고, 세션 공유로 전체 스위트 실행 시간을 18분에서 11분으로 단축했습니다." | "Playwright E2E 구축, 로그인 셋업 *(60줄 → 공통 호출 1줄)*, 세션 공유로 스위트 *(18분 → 11분, 39%↓)*" |
| "EC2에 Claude를 띄워 커밋 변경을 분석하고, 전체 E2E를 매번 돌리는 대신 변경과 관련된 테스트만 자동 선별·실행해 Teams로 보고하는 시스템을 구축했습니다." | "**AI 변경 감지 E2E**: EC2 Claude가 커밋 diff 분석 → 관련 E2E만 선별·실행 → Teams 보고 *(10분 주기 무인 검증)*" |

### 4-3. 판단·지속성 줄 (카드마다 1줄)

불릿 마지막에 **`- **판단·지속성**:`** 한 줄로 트레이드오프나 한계→재설계를 압축합니다. 문단으로 늘리지 않습니다.

- "**판단**: WebSocket이 구현은 빠르나 LNA 재차단 우려로 탈락 → 장기 안정성 보고 Custom Protocol 채택"
- "**지속성**: 빌드타임 통합 후 배포 부담↑ → 다시 iframe 회귀(same-origin). 정답 기술 없이 조직·배포 제약에 맞춰 재선택"

### 4-4. 풀문장 `~했습니다`는 도입부·판단 줄에만

`~했습니다` 종결 문장은 **카드 도입부(배경)와 판단 줄**에서 씁니다. 불릿 본체는 압축형(명사구 + 괄호)을 기본으로 합니다. STAR 풀 스토리는 **`interviews/0X-*.md`(면접 스크립트)에 보존**하고 이력서엔 압축합니다.

### 4-5. ★ 톤 재가공 — interviews/findings 원문을 그대로 옮기지 않는다

findings(사실)·interviews(4축 서사)는 **입력**이지 이력서 본문이 아닙니다. 인터뷰 답변의 1인칭 구어 회고를 직인용하면 톤이 늘어집니다.

| ❌ interviews 원문 직인용 | ✅ 이력서 톤으로 재가공 |
|---|---|
| "통합 전략에 정답 기술은 없고 조직·배포 제약에 맞는 선택이 있을 뿐임을 체득했습니다" | "정답 기술 없음 — 라이브러리 → iframe → 빌드타임 → iframe 회귀, 조직·배포 제약에 맞춰 재선택" |
| "패턴이 푸는 문제를 정확히 이해하지 못한 채 일관성을 명분으로 도입했던 한계를 돌아본 결과입니다" | "패턴 과적용을 겪고 '기술 도입은 우선순위 높은 문제부터'라는 기준 확립" |

> 규칙: interviews 문장이 본문에 거의 그대로 보이면 **재가공 실패**. 구어·회고 어미("~체득했습니다/~돌아봤습니다")를 걷고 명사구·결과 진술로 압축합니다.

### 4-6. ★ 불릿 선별 — 임팩트 없는 미시 구현은 버린다

면접관이 **"그래서?"**라고 되묻는 불릿은 뺍니다. 불릿의 단위는 **임팩트·판단·범위**지, 구현 디테일이 아닙니다.

| ❌ 미시 구현 (의미 약함) | ✅ 버리거나 의미로 승격 |
|---|---|
| "INI/XML·HTTP 로직을 TDD로 구현 (단위 테스트 18개)" | **제거** — 12종 CAM 정합 회귀 보장이 이미 임팩트를 담음 |
| "단위 테스트 22개로 가드" | "Race Condition·Stale Closure 해결" (테스트 개수는 곁가지로 생략) |

- **버릴 것**: 단위 테스트 개수 단독, 파일 포맷 파싱(INI/XML), 사소한 라이브러리 사용, "~도 했습니다"식 곁가지.
- **살릴 것**: 그 작업이 만든 **결과·보장·판단**. 테스트는 "무엇을 보장했나"가 분명할 때만, 개수는 곁들임.

### 4-7. ★ 코드 심볼은 넣지 않는다 — 개념·기법으로 번역

이력서는 코드 리뷰가 아닙니다. **변수·함수·클래스·파일명, 코드 스니펫 표현은 빼고**, 그것이 구현한 **개념·기법·결정**으로 번역합니다. (잘 쓴 이력서들의 공통점 — 기술명은 있어도 코드 심볼은 없음. findings는 코드 심볼이 가득하지만 그건 **입력**이지 본문 표현이 아닙니다.)

| ❌ 코드 심볼 (findings 원문) | ✅ 개념·기법 번역 |
|---|---|
| "dotenv가 process.env를 오염 → getConfig로 전환" | "빌드 시점에 환경변수가 번들에 박히던 구조를 런타임 주입으로 전환" |
| "BusinessLogicError로 throw, .when(code).otherwise() 체인" | "서버가 HTTP 200으로 내리는 비즈니스 에러를 커스텀 에러로 코드별 분기" |
| "BasePage Page Object", "e2e-local-daily 워크플로" | "Page Object 패턴", "로컬 데일리 E2E 워크플로" |
| "BitbucketPublisher 클래스를 패치" | "자동 업데이트 라이브러리 내부를 분석해 패치" |

- **OK (남김)**: 기술·라이브러리 이름(React, Electron, Custom Protocol, Module Federation, FSD), 표준 패턴·개념(ErrorBoundary, Fault Tolerance, 딥링크, 특성화 테스트, Page Object).
- **NO (뺌)**: 내부 변수/함수/클래스/파일명, 코드 스니펫(`statusCode:200`), 사내 고유 식별자(`BASE_DOMAIN`, `config.js`). → **면접 구두용으로 남겨둠.**

---

## 5. 정량화 — 실측만, 없으면 정성

> **철칙: 면접에서 방어 못 할 숫자는 쓰지 않는다.** (= CLAUDE.md "임의 수치·반올림·추정 금지")

**정량 표기법**: `A → B` 화살표를 **괄호로 분리** — `*(1.5초 → 700ms)*`, `*(요청 300개 → 40개)*`. 문장 속에 묻지 않습니다(§4-2).
**FE 지표 옆에는 측정 도구를 1구 덧붙임**: `*(18분 → 11분, 39%↓ · 직접 비교)*`, "Lighthouse/Web Vitals 기준" 등 — 신뢰도가 올라갑니다.

**수치가 없을 때 대체 사다리** (위에서부터 시도):
1. 직접 지표 — %, ms, MB, TC 개수, 빌드 시간, 번들 크기
2. 범위(scope) proxy — 컴포넌트 N개, 제품 N개, 연동 시스템 N개 (설명 가능할 때만)
3. 속도/품질 정성 — turnaround 단축, 에러 알림 빈도 감소, 배포 주기 개선
4. Before→After 상태 — "수동 → 자동", "3중 구현 → 단일 소스"
5. 체감 정성(정직하게) — "에러 알림 빈도가 체감상 줄었습니다"

---

## 6. 동사·서술어와 금지 표현

불릿 본체는 **동작 명사로 시작**하고 **명사구·압축형으로 종결**합니다(§4-1). 카드 **도입부(배경)와 판단·지속성 줄**은 `~했습니다` 경어체. 즉 **블록별로 일관**: 불릿=압축형, 문장=경어체. 한 불릿 안에서 둘을 섞지 않습니다.

| 범주 | 권장 서술어 |
|------|------|
| 설계/구조 | 설계했습니다 / 구조를 잡았습니다 |
| 구축 | 구축했습니다 / 구현했습니다 |
| 주도 | 주도했습니다 / 이끌었습니다 |
| 전환 | 전환했습니다 / 마이그레이션했습니다 |
| 개선 | 최적화·개선·단축·절감했습니다 |
| 통합 | 통합했습니다 / 일원화했습니다 |
| 문제해결 | 원인을 진단·해결했습니다 |
| 표준화 | 표준화했습니다 / 정립했습니다 |

**금지 표현 (즉시 교체)**:
- "참여했습니다 / 담당했습니다(만)" → "전담·주도·설계·구현했습니다"
- "다양한 기술을 사용했습니다" → 구체 기술 + 사용 맥락 + 결과
- "팀과 협업했습니다 / 팀을 도왔습니다" → 구체 기여 범위 + 결과
- 모호한 정량("많이", "다양한", "약 10명", "~6주") → 정확값 또는 정성
- 같은 동사 반복(특히 "구현/담당") → 동사 다양화

---

## 7. 기술 스택은 "나열" 말고 "맥락"에

별도 기술스택 섹션은 **폭(breadth)**, 프로젝트 bullet은 **깊이(depth)** 담당.
bullet 안의 기술명은 항상 *"무엇을 달성하기 위한 수단"*으로 등장해야 합니다.

| ❌ 나열 | ✅ 맥락 |
|---|---|
| "React, TypeScript, Electron 사용" | "Electron 메인/렌더러 IPC 구조를 재설계하여 자동 업데이트 파이프라인을 구축했습니다." |
| "다양한 상태관리 경험" | "전역 상태가 트리 전반에 산재하던 문제를 도메인 단위 store로 분리해 리렌더 범위를 좁혔습니다." |

자기평가 등급("expert/proficient"), day-to-day로 안 쓰는 사소한 도구는 제외.

---

## 8. 미들급 → 시니어 신호 (★ 차별점)

연차는 미들급이지만 **시니어 신호를 섞으면 상향 어필**됩니다. 핵심은 **판단(judgment)과 범위(scope)**.

| 축 | 미들급 서술 | 시니어 지향 |
|---|---|---|
| Scope | 기능/태스크 단위 | 팀/플랫폼 단위 영향 |
| Architecture | "구현했다" | "트레이드오프 끝에 X를 택했다 / 마이그레이션 주도" |
| Leadership | 혼자 처리 | 기술 방향·표준 제시 (직책 없는 기술 리더십) |
| Outcome | "코드가 동작" | 개발 생산성·신뢰성·사용자 지표 개선 |

**두 가지를 bullet에 꼭 한 절씩 넣기** (= 4축 인터뷰의 HOW·지속성 축):

1. **트레이드오프**: "당시엔 빠른 도입을 위해 X를 택했고, 운영 한계를 겪어 Y로 재설계했습니다."
2. **성장 서사·지속성**: "지금도 유지되고 있습니다 / 이후 ~ 이유로 경계를 재조정했습니다."

> 발굴 OK, 발명 NO. 실제로 있었던 판단만. 없으면 정직한 학습 서사로. (CLAUDE.md §3)

---

## 9. 좋은 예 vs 나쁜 예 (실제 사례 인용)

| ❌ 나쁜 (작업 나열·모호) | ✅ 좋은 (성과 서사·정량) |
|---|---|
| "React와 TypeScript로 웹앱을 개발했습니다. Redux로 상태 관리를 구현했습니다." | "고객 이탈률을 15% 낮춘 반응형 웹앱을 개발하고, 컴포넌트 문서화를 주도해 신규 입사자 온보딩을 1주→3일로 단축했습니다." |
| "API 개발을 했습니다" | "전략 패턴을 적용해 코드 유지보수성을 개선했습니다 (변경 지점 격리)" |
| "성능을 개선했습니다" | "쿼리를 최적화해 응답 속도를 1.5초→700ms로 단축했습니다" |
| "검색 시스템 개선에 참여했습니다" | "Elasticsearch 쿼리 튜닝으로 검색 정확도를 62%→89%로 개선했습니다" |
| "우리 팀이 매출 50억 달성" | "팀 매출 50억 중 담당 B2B 부문 18억(36%)을 기여했습니다" |
| "데이터 처리를 최적화했습니다" | "O(n²) 로직에 해시 테이블을 도입해 O(n)으로 단축했습니다" |

**트레이드오프 서술의 교과서** (이현섭 이력서):
> "스펙상 페이지 하나만 개발하면 되는 프로젝트였기 때문에 별도의 프론트엔드 라이브러리는 사용하지 않았습니다."

→ "안 쓴 이유"까지 적는 것이 미들급 판단력의 증거입니다.

**문제의 크기를 먼저 수치로** (우아한형제들):
> 문제해결 서술 7단계 = 문제 정의 → 원인 → **측정(심각성 수치화)** → 해결책 → 과정 → 평가(결과 측정) → 비고.
> 문제 크기를 먼저 박으면 임팩트가 자동으로 따라옵니다.

---

## 10. 6초 스캔 통과 체크리스트 (카드마다 적용)

- [ ] **각 불릿이 한 줄인가** (배경+방법+결과를 한 불릿에 욱여넣지 않음)
- [ ] **수치·효과가 괄호로 분리**됐는가 (문장 속에 묻히지 않음)
- [ ] 첫 불릿이 **가장 강한 임팩트**인가 (정량 우선)
- [ ] 첫 3개 불릿 안에 지표(정량 or 명확한 정성)가 보이는가
- [ ] 불릿의 70% 이상이 정량/명확한 정성 임팩트인가
- [ ] **오너십 언어**("주도/전담")가 있고, 팀 성과와 개인 기여가 분리됐는가
- [ ] 같은 동사 반복이 없는가
- [ ] 기술이 결과/문제에 붙어 있는가 (나열 아님)
- [ ] **카드마다 "판단·지속성" 한 줄**이 있는가 (미들급 차별점, 단 한 줄로 압축)
- [ ] **interviews/findings 원문을 그대로 옮기지 않았는가** (구어 회고 어미 제거, 톤 재가공 §4-5)
- [ ] 정량은 전부 실측인가 (방어 못 할 숫자 없음)

---

## 11. 분량·디자인

| 항목 | 권장 |
|------|------|
| 분량 | 경력직은 핵심 프로젝트 3~5개를 깊게. 1페이지 강박 X, 단 강조 포인트 묻힐 길이는 감점 |
| 폰트 | 굴림·맑은고딕 지양 |
| 색상 | 두세 가지로 제한, 이모지·특수문자 남발 금지 |
| 레이아웃 | 여백 여유, 글머리 기호로 가독성 (단 ATS 대비 과한 2단 컬럼 주의) |
| 제거 | 사진·생년월일·주민번호·장문 자기소개 |

---

## 12. 이 레포에서 재작성할 때의 절차

1. 카드별 `findings/0X-*.md`(사실)와 `interviews/0X-*.md`(4축 서사)를 입력으로 삼는다.
2. 이 가이드 §3 압축 포맷으로 카드 골격을 잡는다 (도입 1~2문장 + 불릿 + 판단 줄).
3. 각 불릿을 §4-1 압축형으로, §4-2 괄호 수치로 쓴다. **방법은 본문, 수치는 괄호.**
4. 카드마다 **판단·지속성 한 줄**을 심는다 (§4-3, 4축 HOW·지속성에서 — 단 한 줄로 압축).
5. **§4-5로 톤 점검**: interviews/findings 원문이 본문에 그대로 보이면 재가공한다.
6. §10 체크리스트로 자가 검수한다 (특히 "한 줄인가 / 괄호 분리 / 원문 재탕 아님").
7. `resume.md`에는 카드의 가장 강한 불릿 3~4개만 더 압축해 옮긴다.
8. 두 문서 간 기간·수치·명칭 일관성을 점검한다.

---

## 13. 공고(JD) → 이력서 매핑 운영법

지원 공고가 정해지면 이력서를 그 공고에 맞춰 조정합니다. 절차:

1. **JD 키워드 추출** — 공고에서 ① 필수 기술 ② 우대 사항 ③ 책임 범위 동사(설계/운영/개선 등) ④ 도메인 키워드를 뽑습니다.
2. **자산 매핑표** — 추출한 키워드를 내 프로젝트·경험과 매칭합니다 (JD 키워드 ↔ 내 카드·bullet).
3. **정렬·격상** — 매칭되는 역량을 ① 핵심역량 요약 앞쪽 ② 관련 카드의 첫 bullet로 끌어올립니다. 카드 선별·정렬(§1-1)도 이 JD 기준으로 다시 평가합니다.
4. **공백 점검** — JD가 강하게 원하는데 내 자산이 약한 영역은, 있는 경험을 정직하게 끌어올리되 **없는 키워드를 억지로 넣지 않습니다** (면접에서 무너짐 — CLAUDE.md §3 발명 금지).
5. **표현 정렬** — JD가 쓰는 용어로 맞춥니다 (예: JD가 "옵저버빌리티"라 쓰면 본문 "모니터링"을 그 용어로).

> **운영 방식**: 사용자가 지원할 회사의 JD를 제공하면, Claude는 위 절차로 **해당 JD에 맞게 `resume.md`·`career-description.md` 내용을 함께 조정**합니다. 과거 분석해둔 공고 키워드(13개 공고)도 이 매핑의 입력으로 재활용합니다.
>
> **JD별 이력서 버전 분기**(공고마다 다른 파일/빌드 산출물)는 Phase 5의 HTML 빌드 단계에서 별도로 설계합니다. 그전까지는 한 소스를 타겟에 맞춰 조정하는 방식으로 운영합니다.

---

## 출처

**한국 IT 채용 가이드**
- 우아한형제들 기술블로그 — 이직초보 개발자의 이력서 만들기: https://techblog.woowahan.com/2531/
- 잡코리아 — 인사담당자가 끝까지 읽는 경력기술서: https://www.jobkorea.co.kr/goodjob/tip/view?News_No=13228
- 원티드 — 합격을 부르는 이력서/경력기술서: https://www.wanted.co.kr/cv/intro
- 점핏(사람인) — 개발자 이력서 작성팁: https://jumpit.saramin.co.kr/contents/450

**실제 공개 사례 (발췌 인용 출처)**
- 이현섭 이력서(토스페이먼츠 FE): https://hyunseob.github.io/resume/
- velog @k-svelte-master — 이력서 빠뜨리지 않으셨나요: https://velog.io/@k-svelte-master/resume-tips
- velog @prettylee620 — 이력서 피드백 기반 고쳐보기
- velog @jwberry1113 — 경력 기술서 작성법
- GitHub codingmonster-tv/Awesome_Resume_Portfolio: https://github.com/codingmonster-tv/Awesome_Resume_Portfolio
- GitHub JSpiner/RESUME: https://github.com/JSpiner/RESUME

**글로벌 best practice**
- Google XYZ formula / Columbia Career Education — strong bullet points
- The Muse — quantify without numbers
- The Tech Resume Inside Out — structure
- Resume Worded — IT action verbs

> 검증 한계: 라인/오늘의집/CJ ENM의 공식 "경력기술서 평가 기준" 1차 문서는 비공개이며, 위 규칙은 일반 한국 IT 채용 통념 + 합격자 회고에서 수렴한 패턴입니다. 랠릿(rallit) 공개 이력서 본문은 클라이언트 렌더링이라 미발췌 — 추가 사례가 필요하면 브라우저 도구로 후속 조사 가능합니다.
