name: Deploy Resume to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - '*.md'
      - 'templates/**'
      - '.github/workflows/deploy-pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install pandoc
        run: sudo apt-get update && sudo apt-get install -y pandoc

      - name: Build HTML files
        run: |
          mkdir -p output
          pandoc resume.md \
            -o output/resume.html \
            --standalone \
            --section-divs \
            --css=resume-style.css \
            -V pagetitle="김진완 이력서 | Frontend Developer" \
            -V lang=ko
          pandoc career-description.md \
            -o output/career-description.html \
            --standalone \
            --section-divs \
            --css=resume-style.css \
            -V pagetitle="김진완 경력기술서" \
            -V lang=ko
          pandoc versions/resume-styleB2.md \
            -o output/resume-b.html \
            --standalone \
            --section-divs \
            --css=resume-style.css \
            -V pagetitle="김진완 이력서 (1페이지 압축형)" \
            -V lang=ko
          pandoc versions/resume-styleC5.md \
            -o output/resume-c.html \
            --standalone \
            --section-divs \
            --css=resume-style.css \
            -V pagetitle="김진완 이력서 (역량 중심형)" \
            -V lang=ko
          pandoc portfolio.md \
            -o output/portfolio.html \
            --standalone \
            --section-divs \
            --css=resume-style.css \
            -A templates/mermaid.html \
            -V pagetitle="김진완 포트폴리오 | Frontend Developer" \
            -V lang=ko

      - name: Copy assets
        run: |
          cp templates/resume-style.css output/
          cp templates/index.html output/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: output

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
