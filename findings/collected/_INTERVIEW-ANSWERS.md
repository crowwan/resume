# 인터뷰 답변 + 귀속 확정 (2026-06-05)

> 사용자 verbatim 답변 보존(인터뷰 룰 #3) + 그에 따른 **이력서 작성 룰**.
> 재작성 시 이 파일을 `_GAP-REPORT.md`와 **함께** 입력으로 삼는다. 발명 NO.

---

## Tier 1 — 확정

### 1. 런타임 config / 도메인 통합 ★새 본인 고유 기여
> 사용자: "팀 공동으로 했고, runtime config를 사용하기 전에 도메인 통합을 위한 urlHelper 라이브러리를 내가 작업했어. 이걸 시작으로 runtime config, 격리환경 등의 작업이 진행된거야."

- **룰**: runtime config 자체 = **팀 공동**. "주도/공동설계" 표현·웹 -90% 수치 **금지**.
- **본인 고유 기여 = 도메인 통합 `urlHelper` 라이브러리** — 이것이 runtime config·격리환경의 **시작점/토대**.
- 카드1 프레이밍: "도메인 통합 라이브러리(urlHelper)를 깔아 그 위에 팀이 runtime config·격리환경을 쌓음" — 본인 = 토대 제공자. (Git Subtree Split 이관도 본인 owner와 결합 가능)

### 2. Rspack 전환 — [기억 불명확]
> 사용자: "내가 했는지 기억이 안나. ... cloud client 빌드 파이프라인에서 타입체크가 터지는 문제가 있었는데 그 문제를 해결하려고 전환했던 기억이 있긴해."

- **룰**: 본인 단독 성과로 **단정 금지**. 확정 슬라이스 `getPublicPath` util만. 동기(빌드 타입체크 실패)는 면접 구두용. → 이력서엔 Rspack 전환 단독 주도 표현 X.

### 3. 결제 팝업앱 (imago-payment-client)
> 사용자: "기존에 사용하던 앱이긴 했는데 여기에 스트라이프를 연동한 결제 페이지를 추가한거야."

- **룰**: 앱 자체 구현은 본인 아님. **본인 기여 = 기존 별도 결제 팝업앱에 Stripe 연동 결제 페이지 추가**(SUB-331 add-seats / SUB-387 business-error). origin 격리는 기존 구조. "결제 앱 본인 구현" 표현 **금지**.

### 4. Export-to-SW / Account 제품 지표
> 사용자: "export to sw 너가 말하는 게 정확히 뭔지 모르겠어. account 제품 지표는 딱히 알 수 있는 게 없어. 뭐 한다고 하면 jira 버그 수로 계산이 가능할지 모르겠는데 의미가 있진 않아."

- **룰**: "Export-to-SW 50건" 프레이밍 **폐기**(Jira 라벨 아티팩트). **Account 제품 지표 없음** → 결제 카드는 "정확성 검증 **구조**(Fault Tolerance·business-error 코드 분기·폴링 안전장치)·구독 워크플로우 전담"으로. **제품 수치 0, 창작 금지** (라인용도 동일).

### 5. blur 휴리스틱 / CAM 수 / NX 선택
> 사용자: "blur 휴리스틱은 아직 해결방법을 고민중인데, 애초에 이 방식으로 머지되어 있어. 개선하려는 거야. cam sw는 12개가 맞을거야 ... nx는 기존 모노레포가 nx로 구성된 상태였고 이걸 다른 모노레포 도구로 바꾸면서 얻는 이득이 크지 않을 것 같았어."

- **blur 휴리스틱(DEN-4466/4503)**: 현재 방식이 **머지된 상태, 개선 검토 중** → Linker "지속성·한계" 서사로(해결 *성과* 아님). custom-protocol이 데이터 전달은 풀었으나 양방향성 포기 → 감지 채널 부채 = 현재 개선 중.
- **CAM SW = 12종** 확정 (현 이력서 '16' 정정).
- **NX = 상속**: 기존 모노레포가 이미 NX. **"전환 이득 < 비용"으로 유지 판단.** → 채널톡 카드: "백지에서 NX 선택"이 아니라 **"전환 비용 대비 이득이 부족해 유지 판단"**(비용-편익 판단력). 발명 금지.

---

## Tier 2 — 답변 확보 (2026-06-05)

### AI 변경 감지 (e2e detect) — 메커니즘 + Teams 실물 증거
> 사용자: "ai 변경 감지는 [스크린샷] 이런 식으로 우리 팀 팀즈로 전달되게 했어. 우리는 QA 팀이 만든 qase의 tc들이 있고 그 tc를 만족시키지 못하면 버그로 처리가 되어서 내가 구성할 때 이 변경된 파일을 보고 연관이 있을 tc를 클로드가 찾아서 관련 테스트를 돌리는 식으로 했던 것 같네."

- **메커니즘**: 커밋 범위 diff → 변경 파일 분석 → **Claude가 QA팀 qase TC 중 연관 TC 자동 선별** → 관련 테스트만 실행 → **Teams 보고** (+ AI가 "실제 회귀 vs 테스트코드 문제" 1차 triage). qase=QA팀 TC 관리, TC 미충족=버그 처리 체계.
- **실물 증거**(2025-12-30 17:41 Teams): 커밋범위 `14782cdfd4→792acc31fb`, 변경파일 milling-client(ProcessingQueue UI)·account-server(Export Session 로직), 실패 TC #365(Export 버튼 selector 못 찾음 — 테스트코드 문제), 스킵 #366/#368, AI 판단 "Export Session API 변경과 무관한 테스트 코드 문제로 보임". → `findings/collected/assets/ai-e2e-detect-teams.png`
- **룰**: "회귀 N건 잡음" 단정 X. **"커밋 diff→연관 qase TC 자동 선별→실행→Teams 보고 + AI triage(실제 회귀/테스트코드 문제 구분)"** 메커니즘으로 기술.

### 격리 재현 환경 — 의도 정정
> 사용자: "실제로 잡았던 버그들은 많아 다만 환경 차이로 안 보였다기보다는 다른 변경사항에 영향없이 재현상황을 만들 수 있는 식으로 돌아가는 데 지금은 이 작업에 대해 여러 고민이 있어. 컨테이너 자체가 무거워지고 있어서 성능 개선도 필요해보이고, 실제로 재현용 이미지와 버그 수정용 이미지 ... 우선 지금까지 파악한내용으로 해도 될 것 같아."

- **의도 정정**: "환경 차이로 안 보이던 버그" 프레이밍 **폐기**. 진짜 의도 = **다른 변경사항의 간섭 없이 특정 재현 상황을 결정론적으로 만드는 것**.
- **지속성·한계(정직)**: 컨테이너가 무거워져 **성능 개선 필요**, **재현용 이미지 vs 버그수정용 이미지 분리** 검토 중. 의도 자체를 더 정리하는 중 → 과대 의미부여 X, 담백하게.

### Taxonomy 7분류 자기폐기 — 판단 서사
> 사용자: "taxonomy는 내가 임의로 추가한 분류인데 너무 특정 앱에 치우쳐진 느낌이 있었어 그래서 다른 팀도 범용적으로 사용하기 위한 규칙을 좀 정해야할 필요가 있었고, 실제 우리팀 컨플루언스에서 데이터독 가시성 문서를 보면 규칙이 어느정도 정해져서 그걸 따르기 위한거였어"

- **서사**: 본인이 임의 추가한 7분류가 **특정 앱 편향** → 팀 전체 범용 규칙 필요 → 팀 Datadog 가시성 문서의 표준 규칙에 맞춰 **자기 분류를 폐기·정렬**. (자기 작업을 비판적으로 축소 = 미들급 판단·협업 어필)
- **귀속 주의**: 어휘/표준 일부는 팀(hckim) → 본인은 표준에 align + 자기 분류 폐기 판단.
